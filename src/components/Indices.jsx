import React from 'react';

const Indices = () => {
  return (
    <div className="flex items-center justify-between mb-4">
      {/* Title Section */}
      <h2 className="text-xl font-semibold">Indices</h2>

      {/* Link Section with Hover Thicker Font */}
      <a href="#" className="text-sm text-gray-600 hover:text-black font-semibold">
        All Indices
      </a>
    </div>
  );
};

export default Indices;
